kind: playground
title: SilverStack Jenkins Server
description: Jenkins LTS on Ubuntu 24.04 with systemd, Nginx reverse proxy, and cloudflared for instant public access via Cloudflare Tunnel - no firewall rules needed.
categories:
    - linux
    - ci-cd
    - containers
playground:
    networks:
        - name: local
          subnet: 172.16.0.0/24
    machines:
        - name: jenkins-server
          users:
            - name: root
            - name: ibtisam
              default: true
          drives:
            - source: oci://ghcr.io/ibtisam-iq/jenkins-rootfs:latest
              format: ext4
              mount: /
              size: 50GiB
          network:
            interfaces:
                - network: local
          resources:
            cpuCount: 4
            ramSize: 10GiB
    tabs:
        - kind: ide
          name: IDE
          machine: jenkins-server
        - kind: terminal
          name: ibtisam-iq
          machine: jenkins-server
        - kind: http-port
          name: "Jenkins UI"
          machine: jenkins-server
          number: 80
    accessControl:
        canList:
            - anyone
        canRead:
            - anyone
        canStart:
            - anyone
