kind: playground
title: SilverStack SonarQube Server
description: SonarQube Community Edition on Ubuntu 24.04 with systemd, Nginx reverse proxy, and cloudflared for instant public access via Cloudflare Tunnel — no firewall rules needed.
categories:
    - linux
    - ci-cd
    - containers
playground:
    networks:
        - name: local
          subnet: 172.16.0.0/24
    machines:
        - name: sonarqube-server
          users:
            - name: root
            - name: ibtisam
              default: true
          drives:
            - source: oci://ghcr.io/ibtisam-iq/sonarqube-rootfs:latest
              format: ext4
              mount: /
              size: 50GiB
          network:
            interfaces:
                - network: local
          resources:
            cpuCount: 4
            ramSize: 10GiB
    tabs:
        - kind: ide
          name: IDE
          machine: sonarqube-server
        - kind: terminal
          name: ibtisam-iq
          machine: sonarqube-server
        - kind: http-port
          name: "SonarQube UI"
          machine: sonarqube-server
          number: 80
    accessControl:
        canList:
            - anyone
        canRead:
            - anyone
        canStart:
            - anyone
