Welcome to SilverStack SonarQube Lab! 🔍

SonarQube is running internally at http://localhost:__SONARQUBE_PORT__
Nginx reverse proxy is listening on port 80 (internal)
PostgreSQL database is running on port 5432 (internal)

Tools:
- cloudflared         Cloudflare Tunnel for public access
- java 21             Modern LTS Java runtime
- postgresql 18       Production-grade database
- arkade  jq  yq  fx  task  just  fzf  btop  cfssl  ripgrep

Default SonarQube credentials:
  Username: admin      Password: admin

To expose SonarQube publicly via Cloudflare Tunnel:

  1. Go to dash.cloudflare.com/one → Networks → Connectors
  2. Create tunnel → choose Cloudflared → name it (e.g. sonarqube-lab) → save
  3. Copy & run the install command shown:  sudo cloudflared service install <token>
  4. Add a published application route:
       Subdomain: sonar   Domain: yourdomain.com
       Service:   HTTP    URL:    localhost:80
  5. SonarQube is now live at https://sonar.yourdomain.com

Quick commands:
  sonar-status  sonar-logs  sonar-restart  sonar-start  sonar-stop
  pg-status     pg-logs     pg-restart     nginx-status nginx-logs  nginx-reload

Full setup docs and source references:
  https://runbook.ibtisam-iq.com/containers/iximiuz/rootfs/setup-sonarqube-rootfs-image
